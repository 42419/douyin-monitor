# 本工具只跑 Linux，所以仓库里一律用 LF。
#
# 这条规则不是洁癖：开发机是 Windows 时 `core.autocrlf=true` 会把 checkout 出来的
# 文件变成 CRLF，而 `deploy/install.sh` 带着 CR 到 Linux 上会直接以
# `bash: $'\r': command not found` 失败——那是一个只能靠"在服务器上试一次"发现的错误。
#
# `text=auto` 让所有文本文件在入库时归一化为 LF，`eol=lf` 让 checkout 出来也是 LF。

* text=auto eol=lf

# 二进制文件不要动
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.gz binary
*.zip binary

# 明确标记，免得将来有人把这一段删了
*.sh text eol=lf
*.service text eol=lf
*.conf text eol=lf
